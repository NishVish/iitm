RewriteEngine On

# Let actual static files serve directly
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Proxy everything else to Node
RewriteRule ^ci/node/(.*)$ http://127.0.0.1:8000/$1 [P,L]

ProxyPassReverse /ci/node/ http://127.0.0.1:8000/