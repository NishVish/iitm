RewriteEngine On
RewriteBase /ci/node/

# 1. If it's a real file (like a CSS file or index.html), serve it directly
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# 2. Everything else goes to the Node app
RewriteRule ^(.*)$ server.js [L]